{
  "name": "ccm-desktop-v2",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "dist": "wails build"
  }
}
